import React, { Component } from 'react';
import './spinner.scss';

export class Spinner extends Component {
    render() {
        return (
            <div className="spinner"></div>
        );
    }
}
